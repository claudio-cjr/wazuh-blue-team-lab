# Sysmon Rules Notes

## Rule ID 92039

Descrição:

```text id="f4q8vn"
A net.exe account discovery command was initiated
```

MITRE:

```text id="m7v2rk"
T1087 - Account Discovery
```

Evento relacionado:

```text id="x1q9wp"
Sysmon Event ID 1
```

Comando utilizado:

```powershell id="p6m4zt"
net user guest
```

---

## Rule ID 92213

Descrição:

```text id="v3q7xn"
Executable file dropped in folder commonly used by malware
```

MITRE:

```text id="k8m1wr"
T1105 - Ingress Tool Transfer
```

Evento relacionado:

```text id="t5q2vk"
Sysmon Event ID 11
```

Comportamento observado:

* criação de arquivo `.ps1`
* PowerShell gerando arquivo temporário
* diretório TEMP monitorado
